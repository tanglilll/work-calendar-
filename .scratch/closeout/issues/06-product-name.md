# 06 — 产品名统一为工作日历

**What to build:** 让文档里的产品名与界面上的一致。界面三处（页面标题、登录页标题、顶栏品牌）已经是「工作日历」，本票把文档侧对齐。

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] 词表首行的产品名改为「工作日历」（保留仓库代号），**只改首行**，领域词汇一条不动 → `CONTEXT.md:1` 由 `# 日程看板（rili）` 改为 `# 工作日历（rili）`；其余各行未动（`git diff` 只有这一行）。
- [x] README 首行带上产品名「工作日历」，不再只有仓库代号与品类描述 → `# rili（工作日历）— 多人实时日程/任务看板`。
- [x] 界面三处保持「工作日历」，不做改动 → 页面标题、登录页 `<h1>`、顶栏品牌三处本就一致，本票未碰 `public/`。
- [x] 历史工单与历史记录里出现的旧称**不改写**——那是当时的观察记录，改了就篡改了证据 → 复核后旧称只剩三处，全在历史工单里：`browser-verification/issues/06` 的证据表原文、以及两处**更正记录**（那两处写着「品牌改名对布局零影响，两者都是 64.00px」）。全部按原样保留。

## 结论

产品名统一为「工作日历」，文档侧两行改动，界面侧本来就是这个名字。历史记录不动。

一处顺带的事实，写下来免得以后误读：旧称「日程看板」与新名「工作日历」都是 4 个全角汉字、同字号，实测宽度都是 `64.00px`——顶栏折行门槛的差异**不是**改名造成的（这一点已在 `browser-verification/issues/06` 的 Comments 与本票关联的工单里更正过）。

## 证据

```
$ head -1 CONTEXT.md   → # 工作日历（rili）
$ head -1 README.md    → # rili（工作日历）— 多人实时日程/任务看板
$ grep -c 工作日历 public/index.html → 3     # 页面标题 / 登录页标题 / 顶栏品牌
$ grep -rn 日程看板 --include=*.md .   → 只剩历史工单的观察与更正记录（3 处，未改写）
```

无测试影响（纯文档）：本票改动后 `npm test` 仍 97 项全绿。
